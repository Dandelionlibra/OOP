package homework1;
import java.util.Scanner;
import java.util.InputMismatchException;

public class homework1_1 {
	private int val;
	
	private static void swap(homework1_1 objA, homework1_1 objB) {
		int tmp = objA.val;
		objA.val = objB.val;
		objB.val = tmp;
	}
	
	public static void main(String[] args) {
		homework1_1 objA = new homework1_1();
		homework1_1 objB = new homework1_1();
		Scanner scanner = new Scanner(System.in);
		
		System.out.println("# This is the first question #");
		
		while(true){
			try{
				System.out.print("Please enter the first number:");
				objA.val = scanner.nextInt();
				
				System.out.print("Please enter the second number:");
				objB.val = scanner.nextInt();
				scanner.close();
				break;
			} catch(InputMismatchException e){
				System.out.println("Please make sure the format is right!");
				scanner.next(); // clear the error input
			}
		}
		
		
		swap(objA, objB);
		System.out.println("After swap: A = " + objA.val + ", B = " + objB.val);
		
		return;
	}
}
